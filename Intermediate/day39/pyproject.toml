[project]
name = "day39"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "requests",
  "requests_cache",
  "python-dotenv",
  "twilio"
]
